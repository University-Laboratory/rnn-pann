# コーディングルール

1. ファイル名は `note<数字>.ipynb` という形式です
2. PANN や GRU などのコードは `src/utils/models.py` をインポートして使う
   1. ただし、研究の過程でモデルを変更、改良などする場合はそれぞれのファイル内でモデルを定義する
3. グラフを描画する際、iL は青系の色、vC は橙系の色を使用する
4. グラフは基本的に線ではなく点で描画する
5. グラフのラベルに日本語を使用しないこと
6. 必ず型アノテーションを使用すること
7. ruff でコードチェックを行うこと
